import React from "react"
const FileB = (props) => {
    return <div>In a GUI (graphical user interface), such as Microsoft Windows, files display as icons that relate to the program that opens the file.</div>
}
export default FileB;